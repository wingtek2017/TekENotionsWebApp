@model IEnumerable<TekENotionsWebApp.Models.needle_sizes>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.metric)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.us)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.hook)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.metric)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.us)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.hook)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
